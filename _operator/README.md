# Operator / archive (not published)

GitHub Pages (Jekyll) does **not** copy folders that start with `_`.

- `initialize.html` — legacy duplicate of the BioCyber hub
- `preload.html` — incomplete stub
- `seals/SEAL69.json` — seal record (was misnamed `.html`)

Public URLs that used to point here now show a noindex stub.

Do not put PATs, `.env`, or private keys here.

Δ9Φ963 · Human remains publisher.
